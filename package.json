{
  "name": "tour-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @tour/web",
    "build": "cd apps/web && /tmp/tour-platform/node_modules/.bin/next build",
    "test": "npm run test -w @tour/web",
    "security:scan-secrets": "node scripts/scan-secrets.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "dotenv": "^17.4.1",
    "pg": "^8.20.0",
    "playwright": "^1.58.2",
    "supabase": "^2.87.2",
    "typescript": "^5.9.3"
  }
}
