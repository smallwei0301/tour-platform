# Browser E2E — GH-1257 conflict-override + guide-warning + single-day opening — 2026-06-06

**Issue:** #1273 — [QA] Run online Playwright E2E for GH-1257 (slices from #1262 / #1270 / #1271)
**Parent feature:** #1257 (CLOSED — fully merged)
**Author:** AI agent (Claude Code)
**Branch:** `claude/issue-1273-gh1257-e2e` (off `origin/main` `47ffc3e`, which includes GH-1257 HEAD `b71fd00`)
**Date / time:** 2026-06-06 ~ Asia/Taipei

---

## Why this issue existed

The Playwright specs for GH-1257 were authored on each PR but the **browser run was
deferred** — local agents hit `NOT_AUTOMATABLE_LOCAL` (≈786 MB MemAvailable, inotify
watches capped at 8192, no `node_modules` → `next dev` risked ENOSPC/OOM). CI (`ci.yml`)
only runs lint/typecheck/unit/build — **it does not run the e2e project** — so these specs
had never actually executed in a browser until now.

This run was performed in a remote environment with ample resources (≈16 GB MemAvailable,
inotify watches ≈130 000), which made the deferred browser layer automatable.

## Environment

| Item | Value |
|------|-------|
| Runner | remote sandbox, Chromium via `@playwright/test` (`npx playwright install chromium`) |
| App under test | local `next dev` on `http://127.0.0.1:3333` (Playwright managed `webServer`) |
| Server env | `NODE_ENV=development VERCEL_ENV=preview`, `ADMIN_ACCESS_TOKEN=test-token-123`, `ADMIN_EMAIL_ALLOWLIST=admin@tour-platform.com`, `GUIDE_SESSION_SECRET=<dev-only>` |
| Data layer | all feature APIs mocked via `page.route(...)` (no Supabase seed); admin session minted through the real `/api/admin/auth/{csrf,session}` routes |

**Note on "online/preview":** these specs deliberately mock the data APIs and mint an admin
session with the dev test token, so they verify the **real rendered pages + middleware +
client behavior** without depending on a seeded backend. The Vercel preview cannot accept
the dev admin token, so the production-equivalent browser run is done against a local
`next dev` build of the merged `main` — the rendered UI is the same bundle. No production or
real booking data was touched.

## Command

```
ADMIN_ACCESS_TOKEN=test-token-123 ADMIN_EMAIL_ALLOWLIST=admin@tour-platform.com \
ADMIN_EMAIL=admin@tour-platform.com GUIDE_SESSION_SECRET=<dev-only> \
npx playwright test \
  e2e/issue1257-admin-conflict-override-ui.spec.ts \
  e2e/issue1273-guide-conflict-override-warning.spec.ts \
  e2e/issue1273-admin-single-day-opening-tz.spec.ts --reporter=list
```

## Result — **4 passed** (0 failed)

| # | Spec | Surface | Result |
|---|------|---------|--------|
| 1 | `issue1257-admin-conflict-override-ui.spec.ts` (test 1) | Admin (#1262) conflict-exception modal: blocked slot → modal → required-reason validation → helper metadata POST (with `x-csrf-token`) → slot refreshes to `allowed_with_admin_override` | ✅ PASS |
| 2 | `issue1257-admin-conflict-override-ui.spec.ts` (test 2) | Admin add-schedule helper copy points to the exception flow | ✅ PASS |
| 3 | `issue1273-admin-single-day-opening-tz.spec.ts` | Admin (#1271) single-day opening under **America/Los_Angeles** browser TZ | ✅ PASS |
| 4 | `issue1273-guide-conflict-override-warning.spec.ts` | Guide (#1270) conflict-override warning + privacy | ✅ PASS |

## Acceptance criteria coverage

- **Admin conflict override (#1262):** verified end-to-end in the browser; the override POST
  carries the helper metadata and a CSRF header, and the slot transitions to
  `allowed_with_admin_override`.
- **Single-day opening, TZ-safe (#1271):** the run forces `timezoneId: 'America/Los_Angeles'`,
  picks `2030-04-12` (a **Friday in Asia/Taipei** = weekday `5`), and asserts the POST body
  sends `weekday: 5` — **not** the LA-shifted `4` (Thursday) a naive `new Date().getDay()`
  would produce. `effective_from === effective_to === '2030-04-12'`, and the rule list echoes
  「單日：2030-04-12」. This is the regression guard for the #1271 noon-anchor weekday fix.
- **Guide warning + privacy (#1270):** the guide booking detail renders the 管理者例外開放 /
  時間衝突 / 需要助手 warning with the guide-visible reason + note. The mocked API deliberately
  **injects an internal `adminNote` sentinel** into the override payload; the test asserts that
  string (and the 管理員備註 row) is **never** rendered — proving the guide UI does not leak the
  internal admin note even if a backend regression forwarded it.

## Spec changes (test-only; no product behavior changed)

- `e2e/helpers.ts` — added `setGuideSession(page, guideId)`: mints a **format-valid** (fake
  64-char signature) `guide_token` + `guide_id` so edge middleware renders `/guide/*`. Real HMAC
  verification still lives in the API routes (mocked here) — auth/RLS were **not** weakened.
- `e2e/issue1257-admin-conflict-override-ui.spec.ts` — fixed two selector bugs that had never
  surfaced because the spec was never browser-run: (a) it must select a preview plan before the
  `例外開放此場` CTA renders; (b) the modal title is the dialog's `aria-labelledby` `<div>`, so
  assertions use `getByRole('dialog', { name })` and are scoped to the dialog to avoid
  page-header/preview-card ambiguity.
- New `e2e/issue1273-guide-conflict-override-warning.spec.ts` and
  `e2e/issue1273-admin-single-day-opening-tz.spec.ts`.

## Gates

`npm run lint` → PASS (exit 0). `npm run typecheck` → PASS (exit 0).

## Verdict

**PASS** — all three GH-1257 surfaces verified at the browser layer, including the non-Taiwan
timezone regression guard and the guide-facing privacy guard. No real payment/booking/payout
data was mutated; no secrets, cookies, tokens, or PII appear in the specs or this report
(the dev `test-token-123` is the documented non-production test value).
