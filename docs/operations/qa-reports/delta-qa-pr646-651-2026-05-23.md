# Delta QA Report — PRs #646/#649/#650/#651 — 2026-05-23

**Tested:** 2026-05-23 ~01:00 CST  
**Tester:** Claudia (automated static + contract verification)  
**Issue:** #653 — [QA] Verify delta checklist for PRs merged after 2026-05-21 cutoff

---

## Deploy Verification

| Field | Value |
|---|---|
| Production SHA | `75c1e97a7e6257d46759bb767c0c4aef142f5c8f` |
| Source | `GET https://tour-platform-nine.vercel.app/api/health` |
| PR cutoff | All four PRs merged 2026-05-21 (before this SHA) |

**PRs confirmed in git history:**

| PR | Merge Commit | Merged At |
|---|---|---|
| #646 Fix GH-619 V2 availability source of truth | `0341756a` | 2026-05-21T07:20:37Z |
| #649 Fix GH-647 payments method schema cache | `20c46441` | 2026-05-21T07:57:29Z |
| #650 Fix GH-648 checkout selected date preservation | `5a9cfefb` | 2026-05-21T08:08:25Z |
| #651 test: align GH-648 legacy booking link expectation | `3bd4c0b3` | 2026-05-21T08:33:31Z |

All four merge commits predate the production SHA — deploy confirmed to include all target PRs.

---

## AC2: #646 V2 Availability Source-of-Truth

### Contract Tests (3 files, run from `apps/web/`)

| Test File | Tests | Pass | Fail |
|---|---|---|---|
| `tests/ui/activity-availability-cache-payload.test.mjs` | 3 | 3 | 0 |
| `tests/ui/activity-availability-intent.test.mjs` | 8 | 8 | 0 |
| `tests/ui/issue619-admin-schedules-legacy-label.test.mjs` | 1 | 1 | 0 |

**All 12 tests PASS.**

Notable passing tests:
- `availability API uses 15/30/60 tiered cache policy`
- `activity detail page keeps ISR shell and does not couple availability to page-level re-render`
- `V2 not-open schedule does not collapse into full badge state`
- `date key extraction avoids UTC midnight rollback for +08 fallback timestamps`
- `checkout/payment callback still enforce strong-consistency conflict semantics`
- `guide schedules page is explicitly legacy-only and points V2 users to /guide/availability`

### Static Walkthrough — `src/lib/availability-v2/activity-day-availability.ts`

- `getV2ActivityAvailability()` queries **`guide_availability_rules`** (line 229) — not legacy `guide_schedules`
- Returns `V2AvailabilityResult` with typed `plans: V2AvailabilityDayPlanRow[]`
- Status enum: `'open' | 'full' | 'not-open'` — no legacy schedule fields leaked through
- Blackout dates fetched from `guide_blackout_dates`; booking conflicts from `bookings` with active statuses

**Verdict: V2 adapter reads V2 source exclusively. No legacy schedule table coupling detected.**

---

## AC3: #650/#651 Checkout Date Preservation

### Contract Tests (2 files)

| Test File | Tests | Pass | Fail |
|---|---|---|---|
| `tests/ui/issue648-checkout-selected-date.test.mjs` | 3 | 3 | 0 |
| `tests/unit/booking-entry-routing.test.mjs` | 3 | 3 | 0 |

**All 6 tests PASS.**

Passing tests:
- `resolvePlanBookingHref preserves selected date for legacy checkout flow`
- `resolveInitialCheckoutSelection selects matching open schedule by date + plan`
- `resolveInitialCheckoutSelection does not silently fallback when selected date unavailable`
- `resolveBookingEntryHref keeps legacy checkout when flag is off`
- `resolveBookingEntryHref routes to booking page when flag is on`
- `resolvePlanBookingHref carries v2 params only when flag is on`

### No-Silent-Fallback Code Evidence — `src/lib/checkout-selection.mjs`

`resolveInitialCheckoutSelection()` behavior when `urlDate` or `urlScheduleId` is provided but no matching open schedule is found:

```js
return {
  selectedScheduleId: '',
  validationError: '你選擇的日期目前沒有此方案可預約，請重新選擇。',
};
```

Both `urlScheduleId` miss path (line 27-30) and `urlDate` miss path (line 43-46) explicitly return `validationError` — **no silent fallback to a different date/schedule**. The only unconditional fallback (`openSchedules.find(...)`) is the zero-URL-params path (user arrived without pre-selecting).

**Verdict: Date preservation is correct; unavailable-date error shown as expected.**

---

## AC4: #649 + #652 Cross-Reference

| Item | Status | Evidence |
|---|---|---|
| #649 Legacy method column (`payment_method` schema) | **PASS** | Static inspection in `daily-qa-2026-05-22.md` |
| #652 ECPay `ON CONFLICT` fix | **PASS** | issue652 4/4 tests PASS, cross-ref `daily-qa-2026-05-22.md` |

No re-execution required — both items were covered and PASS in the prior daily QA run.

---

## AC5: Cross-References

| PR/Issue | Relation |
|---|---|
| #619 | V2 availability source-of-truth (root issue fixed by #646) |
| #621 | V2 launch blocker context; V2 now primary traveler flow per #674 |
| #640 | V2 launch blocker context (slot engine) |
| #645 | Prior daily QA cutoff (pre-2026-05-21) — these PRs fall outside that window |
| #648 | Root issue for checkout date preservation (fixed by #650, tested by #651) |
| #647 | Payments method schema cache (fixed by #649) |
| #652 | ECPay ON CONFLICT regression — fixed by #654, verified separately |

---

## Summary

| AC | Description | Result |
|---|---|---|
| AC2 | #646 V2 availability source-of-truth | **PASS** — 12/12 tests, static verified |
| AC3 | #650/#651 checkout date preservation | **PASS** — 6/6 tests, no-silent-fallback confirmed |
| AC4 | #649 legacy method column | **PASS** — cross-ref daily-qa-2026-05-22.md |
| AC4 | #652 ECPay ON CONFLICT fix | **PASS** — cross-ref issue652 4/4 |

**Total contract tests run: 18/18 PASS, 0 FAIL**

## Verdict: PASS

All four PRs (#646, #649, #650, #651) are verified against their acceptance criteria. Production deploy SHA confirmed to include all target commits. No regressions detected.

## Deferred

Live UX smoke (Playwright E2E against production for the checkout date-preservation flow) deferred — contract tests cover the logic path. Can be added as a follow-up if required by QA policy.
