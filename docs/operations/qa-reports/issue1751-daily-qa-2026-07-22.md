# issue1751 每日 QA 驗收報告 — PR #1750 booking UUID 入口修補（2026-07-22）

- Issue：https://github.com/smallwei0301/tour-platform/issues/1751
- 驗證對象：PR #1750「修正 booking UUID 入口避免 slug-only API 404」
- 測試環境 / Deploy URL：`https://tour-platform-nine.vercel.app`（production）
- Commit SHA：`e6aecf70a9f9c8dec3137515aa6a8e68a8dc21aa`（`/api/health` 實測回傳，與 issue 指定 SHA 一致）
- 測試時間（Asia/Taipei）：2026-07-22 08:48 ～ 09:10
- 測試角色：公開旅客訪客（未登入）；本日不含 guide/admin 主驗證（依 issue 範圍）
- 工具：curl（唯讀 GET）＋ Playwright Chromium 141（真實瀏覽器，桌機 1280px／手機 390px）

## 手動測試清單

| # | 測項 | 結果 | 證據 |
|---|---|---|---|
| 1 | 兩個 booking UUID 頁面可載入、不白屏、無可避免 404 | **PASS** | `/booking/c0000003-…0001`、`/booking/6f8049be-…f2d0` 皆 HTTP 200；無 `?plan=` 時顯示明確繁中引導文案「缺少或無法判定方案參數（plan），請從行程頁重新選擇方案」（設計內 fail-safe，非白屏/錯誤頁）；帶 `?plan=half-day` 時完整 booking 畫面（標題、步驟指示、方案、日期、人數、CTA）正常 |
| 2 | DevTools Network/Console：先走 lookup 解析、不再先打 detail API 吃 404 | **PASS** | Network 序列實錄：`200 /api/activities`（lookup）→ 解析 canonical slug → `200 /api/activities/kaohsiung-chaishan-cave-experience`（UUID c0000003…）／`200 /api/activities/activity-1780446372245`（UUID 6f8049be…）。**0 筆** `/api/activities/<uuid>` 404；console error 0、pageerror 0（桌機＋手機共 4 輪） |
| 3 | lookup API 回可用資料、UUID 直打 detail API 維持 fail-closed | **PASS** | `/api/activities?activityId=<uuid>` × 2 → 200（回傳活動清單，兩個目標 UUID 均在內）；`/api/activities/<uuid>` × 2 → **404 `{"code":"NOT_FOUND","message":"activity not found"}`**，契約未放寬 |
| 4 | canonical slug 解析後 UI smoke（方案/內容區/CTA/載入狀態） | **PASS** | 帶 `?plan=half-day`：標題「高雄柴山探洞體驗｜跟著 Andy Lee 走進城市邊緣的地形秘境 — 預約」、步驟指示（1 行程確認／2 旅客資訊）、方案「半日行程」、日期選擇、人數 stepper（預設 4，註明最少 4 人最多 12 人）皆正常；buttons=5、無 console/network error |
| 5 | 手機寬度（390px）UUID 入口可進頁、版型未壞 | **PASS** | 手機版截圖確認主要內容完整（標題、麵包屑、步驟、行程確認卡、日期與人數控件）；0 console error |
| 6 | `/api/health` 與 `/activities` 基線未回歸 | **PASS** | `/api/health` 200、version=`e6aecf7…`；`/activities` 200、h1「全台灣 7 個私人導遊行程」、0 console error、0 failed request |

## 整合測試清單

- [x] `node --test apps/web/tests/unit/issue1745-booking-activity-resolution.test.mjs` → **6/6 pass**（UUID 先解 canonical slug、slug 直接 detail lookup、未知 UUID 維持 `activity not found`、非 404 detail 失敗不走 list fallback）
- [x] Auth/API fail-closed 補足：匿名訪客可完成公開 booking 入口載入，UUID 直打 detail 仍 404 fail-closed（見手動 #3）
- [x] API/DB/RLS 補足：全程無新增 401/403/500（Network 監聽 0 筆 ≥400 回應，除設計內的 UUID detail 404 對照組）
- [x] Payment 前入口補足：booking 頁可正常進入預約畫面（步驟 1 行程確認），付款前入口未被阻斷
- [x] Guide/Admin seams 補足：全程未觀察到跨 realm API 呼叫或異常 redirect

## 完整回歸測試清單

- [x] `npm test` → 4698 tests / **4695 pass / 0 fail** / 3 skipped（Node v22.22.2）
- [x] `npm run typecheck` → exit 0
- [x] `npm run lint` → exit 0
- [x] Public UI smoke：booking UUID × 2、`/activities`、`/api/health` 全數通過（見上）
- [x] Mobile / console-network：手機 390px 兩個 UUID 頁 + 桌機 network/console 監聽證據皆留存
- 附註：traveler 登入後流程本日未執行（issue 明定非主驗證範圍且無登入需求觸發）；e2e smoke lane 未跑（本日無程式碼變更，全量單元/契約測試已綠）

## 失敗項 / Evidence

無。

## Follow-up issue / owner

無新增 follow-up。

## Go / No-Go

**GO**。理由：PR #1750 的 UUID → canonical slug 解析在生產環境實測生效（兩個目標 UUID 均以 lookup → slug detail 的順序成功載入，無 detail-404 噪音）；UUID 直打 detail API 的 fail-closed 契約維持；桌機/手機瀏覽器 smoke 0 console error；全量回歸 0 fail。
